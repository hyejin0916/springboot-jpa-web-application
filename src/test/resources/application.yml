#spring:
#  datasource:
#    url: jdbc:h2:mem:test # memory mode로 테스트 동작
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create # 애플리케이션 실행 시점에 엔티티 모두 지운 후 다시 생성
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true

# 위 설정이 없어도 SpringBoot가 메모리 모드로 테스트 동작

logging.level:
  org.hibernate.SQL: debug # JPA나 Hibernate가 생성하는 SQL이 모두 보임
#  org.hibernate.type: trace